Production is wired. See PRODUCTION-STATUS.txt

Quick verify: npm run health
Smoke test: https://thefranksstandard.com/auth/register then /ops/test-checkout

Webhook URL: https://rochesyrxiyrxhzmkuwk.supabase.co/functions/v1/stripe-webhook
If payments succeed but orders stay pending:
  1. npm run stripe:webhook  (needs STRIPE_SECRET_KEY — use scripts/set-stripe-secret.ps1)
  2. npm run stripe:reconcile  (also needs SUPABASE_SERVICE_ROLE_KEY)
  3. Dashboard auto-reconciles pending orders on load after reconcile-orders deploys
