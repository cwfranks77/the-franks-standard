# Your operator password for https://yoursite.com/ops — type the SAME value in the browser.
# Build-time: ends up in the public JS bundle. Use a long random phrase; never commit the real .env to git.
# Create a file named .env next to package.json and copy this line into it with your phrase after =.
NUXT_PUBLIC_OPS_ACCESS_KEY=
